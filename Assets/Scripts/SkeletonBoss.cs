using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// swaps colour every X seconds, can only damage and be damaged by the opposite colour - ie when blue can only damage and be damaged by yellow
/// gets stunned when takes damage, then swaps colour
/// only takes damage from behind?
/// takes Y hits to kill
/// killing triggers end game sequence
/// </summary>
public class SkeletonBoss : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
